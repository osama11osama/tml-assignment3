# Private repo — _private/ IS tracked (learning docs, plans, notebooks)

# Secrets
.env
.env.local
!.env.example
*.pem

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Course PDFs (keep locally; cite paths in report)
*.pdf

# Large data and model weights (download from HuggingFace locally)
data/
*.npz
*.pt
*.pth
*.ckpt
*.safetensors
.cache/

# IDE and OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Training outputs (keep structure via .gitkeep when added)
results/checkpoints/
results/runs/
results/logs/*.log

# Checkpoint copies saved without .pt extension
results/archive/*_final
results/archive/trades_b8_resnet18_ep20_final

# Report build artifacts (keep LaTeX sources)
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.bbl
report/*.blg
